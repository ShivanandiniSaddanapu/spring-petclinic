name: Java-workflow

on:
  push:
    branches:
      - main


jobs:
  build:
    - name: my-spc
      runs-on: self-hosted

      steps:
        - name: Gitcheckout
          uses: actions/checkout@v4

        - name: Build java with maven
          run: mvn package  